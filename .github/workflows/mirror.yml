name: Mirror to CERN GitLab

on:
  push:
    branches:
      - '**'

jobs:
  mirror:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Mirror to CERN GitLab
        env:
          GITLAB_PAT: ${{ secrets.GITLAB_PAT }}
        run: |
          trap 'rm -f ~/.git-credentials' EXIT
          git config --global credential.helper store
          echo "https://oauth2:${GITLAB_PAT}@gitlab.cern.ch" > ~/.git-credentials
          git remote add gitlab "https://gitlab.cern.ch/LHCb-RD/uc-etas/eta2mumu"
          git push --mirror -o ci.skip gitlab
